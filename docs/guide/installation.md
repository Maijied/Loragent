# Installation

Loragent v2.0 brings a powerful global CLI interface.

## Quick Install

```bash
npm install -g loragent
```

## Setup

Once installed, synchronize the global agents and MCP servers into your IDEs (Cursor/Claude):

```bash
loragent sync
```

You can discover existing agents across your machine:

```bash
loragent discover
```
